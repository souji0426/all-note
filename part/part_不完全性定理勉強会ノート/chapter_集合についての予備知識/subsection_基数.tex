\documentclass[C:/souji/all-note/note]{subfiles}

\begin{document}

\begin{definition}[E:p8 ,\ K:p12]
  \label{definition:集合の対等関係}
  集合$A,B$に対して$A$から$B$への全単射写像が存在するとき,
  $A$と$B$は\textbfgt{equinumerous}{対等}であるといい,
  $A \sim B$で表す.
\end{definition}
\index{たいとうである@（集合が）対等である}\index{equinumerous}
\RegisterInSymbolList{$\sim$}{definition:集合の対等関係}

\begin{proposition}[E:p8 ,\ K:p12]
  $\mathbb{N}$と整数全体の集合は対等である.
\end{proposition}
\ToDoLater{
  整数全体集合は可算であること.
  素朴集合論の復習で証明したらリンクをはる.
}

\begin{proposition}[E:p8 ,\ K:p12]
  \label{proposition:対等関係は同値関係}
  任意の集合$A,B,C$に対して以下の3つが成立する.
  \begin{itemize}
    \item[1. ] $A \sim A$.
    \item[2. ] $A \sim B$ならば$B \sim A$.
    \item[3. ] $A \sim B$かつ$B \sim C$ならば$A \sim C$. \qedhere
  \end{itemize}
\end{proposition}
\ToDoLater{
  対等関係は同値関係.
  素朴集合論の復習で証明したらリンクをはる.
}

テキストでは単純に「対等である関係が反射的, 対称的かつ推移的であることは容易に確かめられる」と書いてあり,
この記述は少し雑だと思いました.
前の定義を見ると「関係○○が反射的」というのは本来ある集合$A$を用いて「関係○○が集合$A$上で反射的」という風に,
「何の集合上で」かを語るべき述語です.
ここでの対称的と推移的は「どの集合上かは」記述する必要はなかったです.
もちろん何を証明すればよいかが伝わるかどうかで見えれば,
伝わるとは思います.
なので雑というのは,
この「反射的」という言葉遣いに対しての感想なのですが,
なぜ何故書かなかったかも推測することはできます.
かなり細かいことをいえば,
オブジェクトな「関係」とメタな「関係」が公理的集合論には存在します.
実際に存在を保証されている集合$A$を用いて$R\subseteq A^2$となっているような$R$は,
集合でもあるからオブジェクトな「関係」です.
一方この対応関係$\sim$というのは,
同値関係と同じような論理式を満たしはするものの,
上の$R$における集合$A$に相当するものが存在ません.
なぜなら対等関係とはある意味「全ての集合の集合」上の同値関係ということになりますが,
公理的集合論において「全ての集合の集合」とは集合にはなりません（こういうのは真のクラスと呼んだりします）.
なのでそのような説明を避けるために,
ある意味雑に書くしかなかったのでは推測します.


\begin{definition}[E:p8 ,\ K:p13]
  \label{definition:基数・濃度}
  集合$A$に対して$\card ( A )$を,
  任意の集合$B$に対して
  \begin{equation*}
    \card ( A ) = \card ( B ) \leftrightarrow A \sim B
  \end{equation*}
  を満たすものとし,
  \footnote{
    テキストでは「$\card(A)$」は「$\card A$」と書いていますが,
    手書きでの見やすさも考慮して,
    また普段の自分の記法ともあわせて,
    このノートでも「$\card(A)$」と書くことにしました.
  }
  これを$A$の\textbfgt{cardninal numger}{基数}または\textbfgt{cardinality}{濃度}という.
\end{definition}
\index{きすう@基数}\index{cardninal numger}
\index{のうど@（集合の）濃度}\index{cardinality}
\RegisterInSymbolList{$\card ( A )$}{definition:基数・濃度}

ここの基数の定義は（数学的な）基数を始めて見る人にとってはかなり分かりにくいのではないでしょうか？
また逆に一度でも具体的なモノとして基数を定義したことがあるならば,
それこそここに書いてあるように順序数を厳密に定義した上で,
その中で特別な順序数として基数を定めたことがあるならば,
この定義は本当に自分が知っている基数と同じものなのか不安に思うかもしれないです.
著者自身はこの定義で問題ないと書いてあります.
ならば,
この定義のやり方で納得できるよう,
もう少し補足してみます.
この定義の最大の不満点は基数というモノを,
具体的に構成することなく,
その性質でもって定義していることだと思われます.
テキストにある通り,
よくよく考えれば,
私たちは$2$が「何か」は具体的に知らないのに,
$2$のその性質「$1$の次の数である」とか,
関係する事実「$1+1$の演算結果」といったものから捉えて,
上手に$2$を利用できています.
「数」はある種極端な例としても,
そのようなことは数以上に抽象的な数学の議論の中でも行われています.
私が思い浮かぶ例としては「順序対」です.
このテキストでは$x$と$y$の順序対$\op{x}{y}$を,
$\op{x}{y}=\{ \{ x \} , \{ x,y \} \}$でもって具体的に（集合でもって）定義しています.
しかし,
テキストによっては
\begin{equation*}
  \forall x^{\prime}\ y^{\prime}\left(\ \op{x}{y}=\op{x^{\prime}}{y^{\prime}}\ \to\ x=x^{\prime} \land y=y^{\prime} \ \right)
\end{equation*}
をみたす組$\op{x}{y}$のこととして,
つまり順序対が満たしてほしい性質でもって定義しているものもあります（例えば\cite{Basic001}の50ページ,
\cite{Basic007}の14ページ,
\cite{Basic008}の75ページなど）.
単純に平面上の1点の座標の表現としてとか,
直積集合の要素としてとかで順序対を必要としている場合は,
これで十分です.
著者の専門分野的に基数の理解が浅いということはありえないので,
このテキストにおける基数の扱いも,
先の\cite{Basic007}での順序対と同じようなもので,
著者なりの基数についての必要最低限がこの定義だと思います.

\begin{definition}[E:p8 ,\ K:p13]
  \label{definition:集合がおさえられているという関係}
  集合$A,B$に対して,
  ある$B^{\prime}\subseteq B$があって$A \sim B^{\prime}$であるとき,
  $A$ \textbf{dominated by} $B$（$A$は$B$で\textgt{おさえられている}）といい,
  $A \preceq B$で表す.
\end{definition}
\index{おさえられている@（集合がある集合に）おさえられている}\index{dominated by}
\RegisterInSymbolList{$A \preceq B$}{definition:集合がおさえられているという関係}

\begin{proposition}[E:p8 ,\ K:p13]
  $A \preceq B$であるとき,
  $A$から$B$への単射写像が存在する.
\end{proposition}
\ToDoLater{
  AがBにおさえられていればAからBへの単射が存在する.
  素朴集合論の復習で証明したらリンクをはる.
}

\begin{definition}[E:p8 ,\ K:p14]
  \label{definition:基数の大小関係}
  集合$A,B$に対して$A \preceq B$であるとき,
  $\card ( A ) \leq \card ( B )$であるとする.
  \footnote{
    欧米では「大きい（小さい）かまたは等しい」という記号を書くとき,
    イコールを表す部分が「$\leqq$」のように二重線になっているものは使わないと聞いたことがあります.
    Wikipediaにもそう書いてあったり（例えば\cite{Wiki004}とか）.
    また同じ二重線でないものとして「$\leqslant$」も使われるようです.
    これは使用者の好みに寄るのかな.
  }
\end{definition}
\RegisterInSymbolList{$\leq$}{definition:基数の大小関係}

原著では上記の定義は「$\card ( A ) \leq \card ( B )$」で,
和訳では「$\card ( A ) \leqslant \card ( B $」になっています.
これはおそらくこの部分だけでなく,
原著は前者で統一されていて,
和訳は後者で統一されています.
これはなぜかと和訳者である嘉田氏に聞いたところ,
版元の仕様らしいです.
つまり嘉田氏のソースファイルでは「$\leq$」となっていますが,
印刷物として出力したときに「$\leqslant $」となるようです.
今後このノートでは原著にあわせて（特に不都合もなさそうなので）$\leq$で統一することにします.

\begin{proposition}[E:p9 ,\ K:p14]
  任意の集合$A,B,C$に対して以下の2つが成立する.
  \begin{itemize}
    \item[1. ] $A \preceq A$.
    \item[2. ] $A \preceq B$かつ$B \preceq C$ならば$A \preceq C$. \qedhere
  \end{itemize}
\end{proposition}
\ToDoLater{
  AがBにおさえられるという関係は反射的・推移的.
  素朴集合論の復習で証明したらリンクをはる.
}

\PropRef{proposition:対等関係は同値関係}と同様ここもテキストでは,
「「おさえられる」という関係は反射的かつ推移的」と書いてあります.
しかし\PropRef{proposition:対等関係は同値関係}下の注意事項が同じように当てはまることに注意します.

\begin{proposition}[E:p9 ,\ K:p14]
  $A\preceq \mathbb{N}$であることと,
  $A$が高々可算であることは同値.
\end{proposition}
\ToDoLater{
AがNにおさえられればAは可算.
素朴集合論の復習で証明したらリンクをはる.
}

\begin{theorem}[E:p9 SCH\"{O}DER-BERNSTEIN THEOREM,{\ \\}\hspace{3.5cm}\ K:p14 シュレーダー・ベルンシュタイン（Schr\"{o}der-Bernstein）の定理]
  \label{theorem:シュレーダー・ベルンシュタインの定理}
  集合$A,B$と,
  基数$\kappa, \lambda$に対して,
  \begin{itemize}
    \item[(a)] $A \preceq B$かつ$B \preceq A$ならば$A \sim B$.
    \item[(b)] $\kappa \leq \lambda$かつ$\lambda \leq \kappa$ならば$\kappa = \lambda$. \qedhere
  \end{itemize}
\end{theorem}
\index{しゅれーだーべるんしゅたいんのていり@シュレーダー・ベルンシュタインの定理}\index{Schr\"{o}der-Bernstein Theorem}
\ToDoLater{
  ベルンシュタインの定理.
  素朴集合論の復習で証明したらリンクをはる.
}

\begin{theorem}[E:p9 THEOREM 0C,\ K:p14 定理0C]
  \label{theorem:濃度の比較可能定理}
  集合$A,B$と,
  基数$\kappa, \lambda$に対して,
  \begin{itemize}
    \item[(a)] $A \preceq B$または$B \preceq A$の少なくとも一方が成り立つ.
    \item[(b)] $\kappa \leq \lambda$または$\lambda \leq \kappa$の少なくとも一方が成り立つ. \qedhere
  \end{itemize}
\end{theorem}
\ToDoLater{
  濃度の比較可能定理.
  選択公理の復習で証明したらリンクをはる.
}


\begin{notation}[E:p9,\ K:p14]
  \label{notation:アレフ記法}
  $\card ( \mathbb{N} )$を$\aleph_0$で,
  $\card ( \mathbb{R} )$を$2^{\aleph_0}$で表す.
\end{notation}
\RegisterInSymbolList{$\aleph_0$}{notation:アレフ記法}
\RegisterInSymbolList{$2^{\aleph_0}$}{notation:アレフ記法}

\begin{definition}[E:p9,\ K:p15]
  \label{definition:基数の演算}
  集合$A,B$とその基数$\card ( A ) = \kappa, \card ( B ) = \lambda$に対して,
  その演算$+,\cdot$を以下のように定める.
  \begin{itemize}
    \item[1.]
      $A\cap B = \emptyset$のとき,
      $\kappa + \lambda\ =\ \card ( A \cup B )$.
    \item[2.] $\kappa \cdot \lambda\ =\ \card ( A \times B )$. \qedhere
  \end{itemize}
\end{definition}
\RegisterInSymbolList{$+$}{definition:基数の演算}
\RegisterInSymbolList{$\cdot$}{definition:基数の演算}

\begin{theorem}[E:p9,\ K:p15]
  選択公理を仮定する.
  どんな無限集合も可算な無限部分集合をもつ.
\end{theorem}

\begin{proof}
  任意に無限集合をとり,
  それを$A$とおく.
  $\mathcal{A}=\mathcal{P}(A)\setminus \{ \emptyset \}$とおくと,
  $\mathcal{A}$は空でない集合の空でない集合族である.
  よって選択公理より$\mathcal{A}$には選択関数$\map{f}{\mathcal{A}}{A}$が存在する,
  つまり$\forall X\in \mathcal{A}(\ f(X)\in X\ )$をみたす$f$が存在するので,
  そんな$f$を1つとって固定する.
  $a_i\in A$を帰納的に以下のように定める.
  \begin{align*}
    a_0\ &=\ f( A ), \\
    a_{i+1}\ &=\ f(\ A\setminus\{ a_0,a_1,\dots,a_i \}\ )
  \end{align*}
  $A^{\prime}=\SetBar{a_i}{i\in \mathbb{N}}$とおくと,
  $A$が無限集合であることから$A^{\prime}$も無限集合であり,
  明らかに$A^{\prime}$は可算な$A$の部分集合である.
\end{proof}

\begin{theorem}[E:p9 CARDINAL ARITHMETIC THEOREM,\ K:p15 基数算術の定理]
  \label{theorem:基数算術の定理}
  $\kappa \leq \lambda$かつ$\lambda$が無限な基数$\kappa, \lambda$に対して,
  \begin{itemize}
    \item[1.] $\kappa + \lambda\ =\ \lambda$.
    \item[2.] $\kappa \neq 0$ならば$\kappa \cdot \lambda\ =\ \lambda$.
    \item[3.] $\kappa$が無限ならば$\aleph_0 \cdot \kappa\ =\ \kappa$. \qedhere
  \end{itemize}
\end{theorem}
\index{きすうさんじゅつのていり@基数算術の定理}\index{cardinal arithmetic theorem}

\begin{theorem}[E:p10 THEOREM 0D,\ K:p15 定理0D]
  \label{theorem:無限集合の要素の有限列全体はその集合の濃度に一致}
  無限集合$A$に対して,
  $A$の要素からなる有限列全体の集合$\displaystyle \bigcup_{n\in \mathbb{N}}A^{n+1}$の濃度は$\card ( A )$と同じ.
\end{theorem}

\begin{proof}
  任意の$n\in \mathbb{N}$に対して$\card ( A^{n+1} )=\card ( A )$である.
  \begin{sub-block}{{\boldmath $\because$}\,}
    これは$n\in \mathbb{N}$についての数学的帰納法から示せる.
    \begin{itemize}
      \item[]\hspace{-0.5cm}（Basis）$A^{0+1}=A$より明らか.
      \item[]\hspace{-0.5cm}（Induction step） \\
        ある$n\in \mathbb{N}$について$\card  ( A^{n+1} ) = \card ( A )$が成立しているとする.
        その定義から$A^{(n+1)+1}=A^{n+1}\times A$であり,
        \DefRef{definition:基数の演算}から
        \begin{align*}
          \card (A^{n+1}\times A)\ &=\ \card ( A^{n+1} ) \cdot \card ( A ) \\
          &=\ \card ( A ) \cdot \card ( A^{n+1} )
          \intertext{\ThRef{theorem:基数算術の定理}における$\kappa, \lambda$を$\kappa=\card ( A ),\lambda=\card ( A^{n+1} )$とすると,}
          &=\ \card  A^{n+1}\ =\ \card ( A )
        \end{align*}
        よって$\card ( A^{(n+1)+1} ) = \card ( A )$である.
    \end{itemize}
  \end{sub-block}
  すると$\displaystyle \bigcup_{n\in \mathbb{N}}A^{n+1}=A\cup A^2\cup A^3\cup \dots$より,
  \begin{align*}
    \displaystyle \card ( \bigcup_{n\in \mathbb{N}}A^{n+1} )\ & =\ \card ( A ) + \card ( A^2 ) + \card ( A^3 ) +\dots \\
    &=\ \card ( A ) + \card ( A ) + \card ( A )+\dots \\
    &=\ \aleph_0 \cdot \card ( A ) \\
    \intertext{そしてもう一度\ThRef{theorem:基数算術の定理}より}
    &=\ \card ( A ). \qedhere
  \end{align*}

\end{proof}

\begin{example}[E:p10,\ K:p16]
  実数における代数数的数全体の集合の濃度は$\aleph_0$である.
\end{example}

普段研究対象とするようなものでないので,
ここで代数的数について定義しておきます（定義は\cite{wiki003}を参考にした）.

\begin{definition}[]
  \label{definition:代数的数}
  複素数$\alpha$が\textbfgt{algebraic number}{代数的数}であるとは,
  ある整数係数$n$次多項式$f(x)=a_n x^n + a_{n-1}x^{n-1}+ \dots + a_1 x + a_0$（$a_n\neq 0$かつどの$a_i$も整数）があって,
  $\alpha$が$f(x)$の根になっている,
  つまり$f(\alpha) = 0$をみたすことをいう.
  代数的数でない複素数は\textbfgt{transcendental number}{超越数}とよぶ.
\end{definition}

ただテキストでは実数の中での複素数にだけ注目しているように思われるので,
実数の中での代数的数のみに注目して,
そんな数の集合が可算であることを示します.

\begin{proof}
  実数かつ代数的数全ての集合を$\mathcal{A}$とおく.
  この$\mathcal{A}$が可算であることを示す.
  ある$n\in\mathbb{N}$に対して,
  全ての$n$次多項式の全ての実数根を集めた集合を$A_n$とおく.
  つまり,
  \begin{gather*}
    A_0\ =\ \SetBar{ \alpha \in \mathbb{R} }{ \exists f(x)=a_0(\ f(\alpha)=0\ ) }\ =\ \mathbb{Z}, \\
    A_1\ =\ \SetBar{ \alpha \in \mathbb{R} }{ \exists f(x)=a_1 x + a_0(\ f(\alpha)=0\ ) }, \\
    \vdots \\
    A_n\ =\ \SetBar{ \alpha \in \mathbb{R} }{ \exists f(x)=a_n x^n + a_{n-1}x^{n-1}+ \dots + a_1 x + a_0(\ f(\alpha)=0\ ) }, \\
    \vdots
  \end{gather*}
  と定めると,
  $\mathcal{A}=\displaystyle \bigcup_{n\in \mathbb{N}}(A_n)$となる.
  そして各$A_n$は可算集合である.
  \begin{sub-block}{{\boldmath $\because$}\,}
    $n$次多項式は$a_0,\dots,a_n\in \mathbb{Z}$（ただし$a_n\neq 0$）の組み合わせの数だけ存在する.
    つまり$n$次多項式全体の集合の濃度は$\card( \mathbb{Z}\setminus\{ 0 \} \times \mathbb{Z}^{n-1} )$であり,
    これまでの定理を用いて計算を進めると
    \begin{align*}
      \card( \mathbb{Z}\setminus\{ 0 \} \times \mathbb{Z}^{n-1} )\ &=\ \card( \mathbb{Z}\setminus\{ 0 \} ) \cdot \card( \mathbb{Z}^{n-1} ) \\
      &=\ \card( \mathbb{Z} ) \cdot \card( \mathbb{Z} )\ =\ \card( \mathbb{Z} )\ =\ \card( \mathbb{N} )\ =\ \aleph_0
    \end{align*}
    であり,
    つまり$n$次多項式はどれも可算無限に存在する.
  \end{sub-block}
  そして各$n$次多項式の実数根は高々$n$個であり,
  つまり$\card ( A_n )\leq \aleph_0^n=\aleph_0$.
  よって$\card ( A_n )=\aleph_0$である.
  すると$\card( \mathcal{A} )=\aleph_0 \cdot \card( A_n )=\aleph_0 \cdot \aleph_0=\aleph_0$より,
  $\mathcal{A}$も可算集合である.
\end{proof}



\end{document}
