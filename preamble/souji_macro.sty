%ソースコードやPDFでの見映えを整えるためのマクロ--------------------------------------------

%%太字命令の簡略化
\newcommand{\textgtbf}[2]{\textgt{#1}（\textbf{#2}）}
\newcommand{\textbfgt}[2]{\textbf{#1}（\textgt{#2}）}

%%各種参照
\newcommand{\PartRef}[1]{第\ref{#1}部（\pageref{#1}ページ）}
\newcommand{\ChapRef}[1]{第\ref{#1}章（\pageref{#1}ページ）}
\newcommand{\SecRef}[1]{\ref{#1}節（\pageref{#1}ページ）}
\newcommand{\DefRef}[1]{Definition \ref{#1}（\pageref{#1}ページ）}
\newcommand{\ExRef}[1]{Example \ref{#1}（\pageref{#1}ページ）}
\newcommand{\LemRef}[1]{Lemma \ref{#1}（\pageref{#1}ページ）}
\newcommand{\ThRef}[1]{Theorem \ref{#1}（\pageref{#1}ページ）}
\newcommand{\FactRef}[1]{Fact \ref{#1}（\pageref{#1}ページ）}
\newcommand{\PropRef}[1]{Proposition \ref{#1}（\pageref{#1}ページ）}
\newcommand{\PuzzleRef}[1]{Puzzle \ref{#1}（\pageref{#1}ページ）}
\newcommand{\TableRef}[1]{表 \ref{#1}}

%%再掲環境
\newenvironment{re-puzzle}[1]{\begin{itembox}[l]{\textbf{Puzzle}\ \ref{#1}の再掲}}{\end{itembox}}

%%証明中の部分証明環境
\definecolor{BIP-back}{gray}{0.9}
\newenvironment{sub-block}[1]
  {
    %\vspace{-5pt}
    \begin{tcolorbox}[
    colback = BIP-back,
    colframe = white,
    boxrule = 10pt,
    fonttitle = \bfseries,
    breakable = true]
    \begin{itemize} \item[#1]
  }
  {
    \end{itemize}
    \end{tcolorbox}
    \vspace{-5pt}
  }


%基本記号------------------------------------------------------------------------------------------
%%ギリシャ数字
\newcommand{\glOne}{I}
\newcommand{\glTwo}{I\hspace{-.1em}I}
\newcommand{\glThree}{I\hspace{-.1em}I\hspace{-.1em}I}
%%定義する両矢印
\newcommand{\defarr}{\ \mbox{$\stackrel{\text{def}}{\Longleftrightarrow}$}}
%%定義する等号
\newcommand{\defeq}{\ \mbox{$\stackrel{\text{def}}{=}$}}

%基礎数学------------------------------------------------------------------------------------------
%%外延的記法の集合
\newcommand{\SetBar}[2]{
  \{\,#1\,\mid\,#2\,\}
}
\newcommand{\SetColon}[2]{
  \{\,#1\,:\,#2\,\}
}
%%写像
%%%写像の記法
\newcommand{\map}[3]{
  #1\colon #2 \to #3
}
\newcommand{\InjectionMap}[3]{
  #1\colon #2 \xrightarrow{\text{1-1}} #3
}
\newcommand{\SurjectionMap}[3]{
  #1\colon #2 \xrightarrow{\text{onto}} #3
}
\newcommand{\BijectionMap}[3]{
  #1\colon #2 \xrightarrow[\text{onto}]{1-1} #3
}
%%%写像の定義域の制限
\newcommand{\restric}{\mbox{$\upharpoonright$}}
%%%定義域や値域など
\DeclareMathOperator{\dom}{dom}
\DeclareMathOperator{\ran}{ran}
\DeclareMathOperator{\fld}{fld}
%%%定義域・値域が共通の関数の集合
\newcommand{\MapSet}[2]{\mbox{}^{#1}#2}
%%%恒等関数
\DeclareMathOperator{\id}{id}
%%%定義域が有限な関数
\newcommand{\FinitlyDomeinFunc}[2]{[ #1 \mapsto #2]}



%%順序対
\newcommand{\op}[2]{\langle #1,#2 \rangle}
\newcommand{\triple}[3]{\langle #1,#2,#3 \rangle}
\newcommand{\ntuple}[2]{\langle #1,\dots,#2 \rangle}
\newcommand{\singleseq}[1]{\langle #1 \rangle}
%%濃度
\DeclareMathOperator{\card}{card}

%論理学--------------------------------------------------------------------------------------------
%%特別な集合
\newcommand{\SetOfSymbol}{\mathrm{SIMB}}
\newcommand{\SetOfSentenceSymbol}{\mathrm{PVAR}}
\newcommand{\SetOfExpression}{\mathrm{EXPR}}
\newcommand{\SetOfWff}{\mathrm{WFF}}
%%トートロジー的に同値
\newcommand{\TautoEquiv}{\models $\rotatebox[origin=c]{180}{$\models$}$}



%公理的集合論-------------------------------------------------------------------------------------
%%有限部分関数全体の集合
\newcommand{\FinitePartialFunctions}[2]{\mbox{}^{<#1}#2}
%%カントール空間
\newcommand{\CantorSp}{\mbox{}^{\omega}2}
%%ベール空間
\newcommand{\BaireSp}{\mbox{}^{\omega}\omega}

%無限組み合わせ論---------------------------------------------------------------------------------
%%posetのコンディションが両立不可能
\newcommand{\compatible}{\not\perp}

%帽子パズル関係-----------------------------------------------------------------------------------
\newcommand{\CanSee}[1]{\raisebox{-0.4ex}{\mbox{$\overrightarrow{#1}$}}}

%自作ツール用-------------------------------------------------------------------------------------
\newcommand{\RegisterInSymbolList}[3]{}

\newenvironment{kongo}{\begin{itembox}[l]{今後の課題}}{\end{itembox}}
